# Спецификация проекта: MVP «Нейро-СММ Ассистент» (v2)

> Обновлённая версия исходной спецификации после разбора и уточнения архитектуры. Изменения и решения, зафиксированные в этой версии, отмечены по ходу документа.

## 1. Целевое назначение и концепция проекта

**Цель:** веб-приложение для автоматизированного анализа малого бизнеса, мастеров и экспертов, распаковки их целевой аудитории и экспертности, а также генерации уникального контента (посты, визуал, сценарии Reels) без типичных нейросетевых шаблонов («нейрослопа»).

**Целевая аудитория (ЦА):** мелкий бизнес (небольшие ветеринарные клиники, локальные магазины), мастера сферы красоты и хендмейда, частные эксперты (коучи, психологи, консультанты) — у которых нет времени, бюджета или глубоких знаний для самостоятельного ведения социальных сетей.

**Статус проекта:** учебный/некоммерческий на текущем этапе — это определяет выбор недорогих моделей и self-hosted инфраструктуры без лишних внешних сервисов.

---

## 2. Точка входа и защита демо-доступа

Проект публикуется на сервере без открытой регистрации — вход только через управляемый процесс, чтобы исключить случайных посетителей и не нести расходы на бесконтрольные запуски (каждый прогон дёргает платные LLM- и медиа-API).

**Лендинг.** Публичная страница с описанием продукта, примерами результатов и (в перспективе, требует отдельной проработки) обучающим интерактивом. Кнопка «Получить демо-доступ».

**Процесс выдачи доступа:**
1. Пользователь оставляет **только email** (решение сессии 2026-09-03, пересматривает более раннее решение той же сессии про выбор канала email/Telegram/ВК) — плюс необязательное имя (минимальные лид-данные для будущей связи/предложений — не полные перс. данные, максимум имя). Причина отказа от выбора канала: email есть у всех, не нужно решать, писать ли клиенту в Telegram/ВК (оба технически не умеют писать первыми — см. ниже), и это же адрес, на который позже можно написать с обратной связью или новым предложением. Не путать с own-соцсетями клиента на онбординге (Telegram/ВК для контент-анализа) — это независимая часть продукта, тут ничего не меняется.
2. Заявка приходит оператору в Telegram с кнопками «Одобрить»/«Отклонить» — **на старте подтверждение ручное**, без автоматики и без антибот-инфраструктуры, так как объём заявок на этом этапе небольшой.
3. По нажатию «Одобрить» генерируется индивидуальная **одноразовая magic-ссылка** и **уходит клиенту автоматически на email** — ручной остаётся только сама модерация заявки (решение/предохранитель от бесконтрольных платных прогонов), не доставка ссылки. Реализовано (`backend/src/lib/email.ts`, универсальная SMTP-обёртка через `nodemailer` — конкретный провайдер не выбран, любой сервис с SMTP-доступом подключается через `.env` без правки кода); при неудаче/отсутствии настройки оператор по-прежнему видит ссылку для ручной пересылки (`delivered: false`), ничего не ломается. Отдельная автодоставка через Telegram/ВК больше не нужна как задача — раз канал теперь единственный, вопрос deep-link/VK Callback API снят (был бы актуален только при возврате к выбору канала).

**Механика защиты ссылки:**
- Ссылка «сгорает» не по IP, а по факту первого использования (первое открытие / первая отправка онбординг-формы) — сервер помечает токен использованным, повторный переход по той же ссылке отклоняется для всех.
- Привязка к IP **не используется** — мобильный интернет часто меняет IP при переключении сети (CGNAT, переключение вышек/Wi-Fi↔4G), и IP-lock рискует заблокировать самого демо-пользователя посреди сессии.
- После первого открытия выдаётся сессионный токен/кука, который держит пользователя внутри демо до его завершения.
- Ссылка ограничена по времени жизни (например, 48 часов), чтобы неиспользованные ссылки не копились бессрочно.
- **Отдельная вторая ссылка** — read-only, на страницу с готовыми результатами. Живёт дольше исходной, не позволяет повторно запустить пайплайн (и, соответственно, повторно потратить бюджет на генерацию), но даёт клиенту вернуться посмотреть или переслать результат.

---

## 3. Архитектурный пайплайн обработки пользователя

### Шаг 1. Онбординг, сбор сырых данных и анализ конкурентов

- **Ввод данных:** ссылки на свои соцсети (Telegram, ВК — для MVP).
- **Анализ конкурентов:** 2–3 ссылки на конкурентов/лидеров мнений в нише.
- **Опросник:** короткая форма о продукте/бизнесе — закрывает минимально необходимый ввод для автономного прохода Фазы 1 в `audience-unpacker` и `expertise-unpacker` (B2C/B2B, описание не продукта, а человека/клиента; 2–3 вопроса-заменителя самых важных пунктов интервью — реальные фразы клиентов, если есть; путь самого эксперта — как пришёл в дело, на чём строится опыт [добавлено позже остальных, закрывает структурную дыру — см. «Прогресс реализации», Блок 2 «Экспертность и путь» `expertise.md` не имел источника данных вообще]; главный принцип работы; что точно не делает в контенте).
- **Авто-анализ стиля:** парсинг постов собственного профиля (ритм, лексика, длина предложений, стоп-слова) и визуальной айдентики.

**Технический способ парсинга (решено):**
- **Telegram** — через публичное веб-превью `t.me/s/<channel>`. Официальный Bot API не даёт доступа к истории чужого канала (бот не является админом канала конкурента), поэтому используется публичная HTML-страница превью — не требует токенов и авторизации.
- **ВК** — через официальный `wall.get` по открытым сообществам, без дополнительных прав от владельца. Требует сервисный ключ доступа VK-приложения (`VK_SERVICE_TOKEN` в `.env`) — заведён и проверен живым вызовом (2026-08-30), см. «Следующие шаги» в разделе 9.
- **Шапка профиля** (аватар/обложка/описание, не посты) — добавлено позже для `profile-header-analyzer` (см. раздел 9): ВК через `groups.getById` (не `wall.get`), Telegram — из того же HTML-превью `t.me/s/<channel>`, других классов, чем у постов.

### Шаг 2. Глубокая аналитика и распаковка (агенты)

Работает **всегда в автономном режиме**, без пошагового диалога с пользователем (форма → кнопка «запустить анализ», не чат):
- Интерактивные ветки промптов (Фаза 0 «проверка прошлых работ», пошаговые интервью) не используются — вместо этого промпты получают на входе весь массив данных онбординга сразу и работают как автономная реконструкция.
- Жёсткие hard-stop'ы («полная остановка при отсутствии данных») отключены — вместо блокировки агент делает лучший возможный проход и помечает слабые места `[требует уточнения у эксперта]`.
- Итеративные уточняющие циклы (например «принцип выглядит банальным, уточни ещё раз») убраны — один проход, агент сам решает, что есть.
- **Разметка источников и честные статусы (`боевой`/`черновик-рамка`/`черновик-скелет`) сохраняются без изменений** — это ключевой элемент доверия к продукту, показывает клиенту, что система не выдаёт нейрослоп за подтверждённый факт.
- Финальная самопроверка (Фаза 4 в `expertise.md`) сжимается до одного самоконтрольного абзаца в выводе, без повторных циклов доработки.
- Оба промпта (`prompts/expertise.md`, `prompts/target-audience.md`) содержат явную развилку режимов «интерактивный диалог / автономная реконструкция» прямо в тексте — не только как внешнее соглашение в этом документе. Глубина и структура фаз не сокращены: содержимое промптов сознательно не упрощается для MVP (см. тест из раздела 4 — упрощение здесь воспроизвело бы ровно тот провал, который показал DeepSeek на полном `expertise.md`).

**Распаковка целевой аудитории** (`audience-unpacker`) — сегментация, боли, «живой язык» с учётом данных конкурентов → документ «Профиль ЦА».

**Распаковка экспертности** (`expertise-unpacker`) — авторский метод (шаги, принципы, уникальные инструменты), табу → документ «Распаковка экспертности».

**Данные клиента:** все собранные данные (стиль, ЦА, метод, табу, референсы, инсайты конкурентов) хранятся как записи в SQLite, привязанные к `client_id` (см. раздел 7). Решение скорректировано по результатам живого теста `@modelcontextprotocol/server-memory` (2026-08-29): сервер по умолчанию пишет в один общий файл на весь компьютер (без привязки к проекту — при тесте в графе обнаружилась чужая сущность из не связанного проекта), изоляция включается только через `MEMORY_FILE_PATH`, и даже с ней это один stdio-процесс с одним файлом без блокировок при параллельной записи — не подходит как продакшн-хранилище для веб-бэкенда, обслуживающего много клиентов одновременно. Сервер остаётся подключён в `.mcp.json` (см. раздел 7), но только как память самого Claude Code между сессиями разработки — не как хранилище клиентских графов.

**account-packager** — синтезирующий агент поверх выхода обоих unpacker'ов + анализа своего профиля: сжимает объёмные документы в практичный набор — короткое позиционирование, готовый текст био, единый tone of voice, рекомендации по визуальному стилю (палитра/свет/композиция — текстовый синтез из ЦА/метода/позиционирования, без анализа фото; решение сессии 2026-09-02, см. раздел 9) и персонализированные (не статичный чек-лист) рекомендации для Шага 3 «Аудит профиля».

### Шаг 3. Стратегия и планирование

- **Аудит профиля** — рекомендации по шапке/контактам, выход агента `account-packager`.
- **Контент-план на 2 недели** — генерируется как план **тем и заголовков по дням и платформам** (Telegram: посты; ВК: посты + темы Reels), без полного текста и без медиа на все 14 дней.
- **Идеи из анализа конкурентов** — `content-planner` получает на вход не только ЦА/экспертность/стиль, но и паттерны от `competitor-analyzer` (топовые по вовлечённости темы и форматы — для ВК ранжирование по `wall.get` лайкам/репостам/просмотрам, для Telegram по счётчику просмотров с `t.me/s/`). Темы, отталкивающиеся от такого паттерна, помечаются в плане открыто (например «формат подтверждён в нише») — не копирование чужого текста, а адаптация подтверждённого формата под метод и tone of voice конкретного клиента. Сохраняет честную линию продукта (см. статусы `боевой`/`черновик-*`) — план не выглядит как выдуманный с нуля, если по факту частично оперся на данные.
- **Демо-контент** (ради демонстрации качества потенциальному заказчику) — из всего плана **полностью генерируются только позиции по реальным площадкам клиента**: до 3 при обеих площадках — 1 пост Telegram (текст + изображение), 1 пост ВК (текст + изображение), 1 сценарий Reels для ВК (текст + видео); меньше, если площадка одна (Reels — только у клиентов с ВК, см. раздел 9, «Клиент с одной площадкой» — решение этой сессии, план не выдумывает контент для площадки, которой у клиента нет). Остальные дни плана остаются на уровне тем/заголовков — это объём следующей итерации после демо, не MVP.

### Шаг 4. Генерация мультимодального контента (для 3 демо-позиций)

- **copywriter** — тексты постов по плану, в соответствии со стилем/графом знаний/табу.
- **visual-generator** — промпты для иллюстраций без стоковых клише, держит визуальный стиль из раздела «Визуальный стиль» `account-packager` (не референсы клиента — решение сессии 2026-09-02 заменило прежний источник, см. раздел 9), передаются в `generate_image` (smm-mcp, Seedream + FLUX.2 Max fallback через OpenRouter) **только по подтверждению пользователя** (см. ниже).
- **reels-writer** — сценарий (хук на первые 3 секунды, раскадровка, текст озвучки), без референсов — на момент генерации у клиента их ещё нет (см. ниже). Передаётся в `generate_video` (smm-mcp, kwaivgi/kling-v3.0-pro через OpenRouter) **только по подтверждению пользователя**.
- **Референсы для рилса — после сценария, не до него (решение сессии 2026-09-02).** Раньше референсы грузились на онбординге вслепую, до того как клиент видел сценарий, и `reels-writer` пытался сам угадать, какая из категорий подходит. Теперь порядок обратный: клиент видит готовый сценарий и уже под него, на той же странице кабинета, добавляет фото, если они есть и подходят. Пока это только хранение (`reels_reference_files`, `POST/GET /api/reels-references`) — не анализируются `visual-style-analyzer`, не передаются в `generate_video` как `reference_image`; wiring в реальную видео-генерацию — отдельная будущая задача (см. раздел 8).
- **editor-in-chief** — проверка каждого сгенерированного текста перед показом пользователю (см. раздел 5).

**Гейт подтверждения перед медиа-генерацией (для всех 3 демо-позиций):** текст/сценарий — дешёвый вызов, генерируется сразу и без подтверждения. Дорогой вызов (`generate_image`/`generate_video`) отделён от этого шага: пользователь сначала видит текст поста или сценарий рилса и то, какие референсы к нему подтянулись (если есть), и только по явному клику «Сгенерировать» запускается вызов медиа-модели. Причина — защита от случайного расхода бюджета OpenRouter на тестовых прогонах, где иначе каждый повторный запуск пайплайна автоматически жёг бы деньги на картинки и видео.

---

## 4. Агенты и модели

| № | Агент | Модель | Роль |
|---|---|---|---|
| 1 | `audience-unpacker` | DeepSeek V4 Pro (`deepseek/deepseek-v4-pro`) *(было Claude Sonnet 5 — см. сноску ниже)* | Распаковка ЦА (`target_audience.md`) |
| 2 | `expertise-unpacker` | DeepSeek V4 Pro (`deepseek/deepseek-v4-pro`) *(было Claude Sonnet 5 — см. сноску ниже)* | Распаковка экспертности (`expertise.md`) |
| 3 | `account-analyzer` | DeepSeek V4 Flash (`deepseek/deepseek-v4-flash`) | Анализ своего аккаунта (стиль, контент) |
| 4 | `competitor-analyzer` | DeepSeek V4 Flash (`deepseek/deepseek-v4-flash`) | Анализ конкурентов, поиск паттернов и топовых по вовлечённости тем/форматов |
| 5 | `account-packager` | DeepSeek V4 Flash (`deepseek/deepseek-v4-flash`) | Позиционирование, tone of voice, био, **визуальный стиль** (палитра/свет/композиция — решение сессии 2026-09-02, см. ниже), аудит шапки |
| 6 | `content-planner` | DeepSeek V4 Pro (`deepseek/deepseek-v4-pro`) *(было DeepSeek V4 Flash — решение сессии 2026-09-02, см. раздел 9: пользователь забраковала связность реального вывода)* | Контент-план на 2 недели (темы), с открытой пометкой тем, опирающихся на паттерны конкурентов |
| 7 | `copywriter` | DeepSeek V4 Pro (`deepseek/deepseek-v4-pro`) *(см. content-planner выше)* | Тексты 3 демо-постов |
| 8 | `visual-generator` | DeepSeek V4 Flash (`deepseek/deepseek-v4-flash`) | Промпты для картинок → `generate_image` (по подтверждению пользователя) |
| 9 | `reels-writer` | DeepSeek V4 Pro (`deepseek/deepseek-v4-pro`) | Сценарий рилса (с fallback на нейтральный топик без референсов) → `generate_video` (по подтверждению пользователя) |
| 10 | `editor-in-chief` | DeepSeek V4 Pro | Проверка на табу/стоп-слова/нейрослоп-паттерны |

*Строка 9 изначально указывала просто «DeepSeek V4» без суффикса — на OpenRouter реально существуют только `-flash` и `-pro` варианты (проверено запросом к `/api/v1/models` перед реализацией). Решено в пользу Pro: раскадровка и хук на первые 3 секунды — более творческая задача, чем обычный пост, ближе по требованиям к `editor-in-chief`, чем к остальным Flash-агентам.*

**Интеграция:** все текстовые агенты — через OpenRouter API (единая точка интеграции, как и медиа-генерация через существующий `smm-mcp`), не через подписку Claude Code — так как продукт задуман как самостоятельный веб-сервис для сторонних пользователей, а не инструмент, который оператор вручную прогоняет через Claude Code. Слаги моделей на OpenRouter идут с префиксом провайдера (`anthropic/claude-sonnet-5`, не голый `claude-sonnet-5`); формат запроса/ответа — OpenAI-совместимый chat completions (`choices[0].message.content`), не нативный Anthropic Messages API.

### Обоснование распределения моделей (по результатам теста)

Проведён контролируемый тест: `expertise.md` целиком (система) + один и тот же синтетический тестовый кейс эксперта (материалы) отправлены в DeepSeek V4 Pro и Claude Sonnet 5 через OpenRouter.

**Результат:** язык и честность разметки источников — на одном уровне у обеих моделей, без выдумывания фактов. Но DeepSeek V4 Pro систематически **пропускал сложные мета-шаги** промпта, не упираясь в лимит токенов:
- не предлагал обоснованный выбор структуры метода (Шаг 2.6);
- не генерировал обязательные варианты названия метода, когда название не финализировано;
- не сверял «уникальные» инструменты с общей практикой ниши (Шаг 2.4) — пропустил очевидную параллель с техниками КПТ, хотя в материалах прямо была подсказка (эксперт сертифицирована по КПТ);
- пропустил финальную самопроверку по 5 критериям (Фаза 4);
- не дал обоснования к пунктам табу, хотя промпт явно требует.

Claude Sonnet 5 выполнил все эти шаги полно и по существу. Стоимость прогона: DeepSeek — $0.0225, Claude — $0.0815 (разница ×3.6, но в абсолютных цифрах это разовая операция на клиента, не на каждый пост — экономия здесь менее значима, чем на агентах с большим объёмом вызовов).

**Вывод (на момент теста):** Claude — на двух unpacker-агентах (ядро продукта, глубина критична), DeepSeek V4 (Flash/Pro микс по задачам) — на остальных, где задачи более рутинные/объёмные и экономия реально накапливается.

*Если нужно — можно повторить аналогичный тест на `target_audience.md` перед продакшн-фиксацией.*

**Решение сессии 2026-09-01 (переопределяет вывод выше для MVP/демо-этапа):** `audience-unpacker` и `expertise-unpacker` переведены на DeepSeek V4 Pro (`deepseek/deepseek-v4-pro`), несмотря на описанный выше контролируемый тест не в её пользу. Обоснование пользователя: на MVP/демо-этапе глубина анализа и так упирается в короткую анкету онбординга (5 полей, не 30–60-минутное интервью), а не в модель — если демо-режим не даёт глубины в любом случае, платить за Claude Sonnet 5 нет смысла. Качество Claude Sonnet 5 на этой паре агентов уже подтверждено прошлыми живыми тестами (см. раздел 9) — это точка отсчёта, если решение придётся откатывать. Решение провизорное: если живые тесты на реальных клиентских данных покажут у DeepSeek те же пропуски сложных мета-шагов, что и в контролируемом тесте выше (обоснование выбора структуры метода, обязательные варианты названия, сверка «уникальных» инструментов с практикой ниши, финальная самопроверка, обоснования табу) — вернуть оба агента на Claude Sonnet 5. `visual-style-analyzer`/`profile-header-analyzer` это решение не затрагивает (vision-задачи, DeepSeek-vision недоступен по data-policy причинам — см. «Известные грабли» в CLAUDE.md).

**Похоже, провал уже произошёл живым тестом (найдено 2026-09-03) — но не в предсказанной форме.** У тестового клиента «держим планку» (`landing-hero-yoga-client`) `expertise_profiles` версии 1 содержит не документ и не поверхностный анализ, а буквально галлюцинированный вызов инструмента: `<tool_calls><tool_call name="web_fetch">{"url": "https://t.me/derzhimplanku", ...}</tool_call></tool_calls>`. Бэкенд не передаёт в OpenRouter параметр `tools` вообще (`backend/src/lib/openrouter.ts` — голый `{model, messages}`), так что DeepSeek V4 Pro выдумал синтаксис инструмента, которого физически нет, вместо того чтобы писать текст. Это более серьёзный отказ, чем «пропуск сложных мета-шагов» из контролируемого теста — не деградация качества, а полный отказ выдать документ; `parseFrontmatter` не нашёл там ничего парсибельного, код подставил дефолтный статус `черновик-рамка`, и обрывок ушёл клиенту как «финальный» документ. Решение принято (2026-09-03): возврат на Claude Sonnet 5 будет сделан, но не сейчас — перед деплоем (раздел 8).

---

## 5. Агент-редактор (editor-in-chief)

Отдельный проверяющий проход перед показом контента пользователю — прямое продолжение ключевого УТП продукта («не нейрослоп»): генеративные модели регулярно теряют часть одновременных ограничений (стиль + табу + стоп-слова) при творческой генерации, отдельная проверка ловит то, что было пропущено при генерации.

**Вход:** сгенерированный текст; блок табу из «Распаковки экспертности» (с обоснованиями); стоп-слова и tone of voice сегмента из «Профиля ЦА»; tone of voice/позиционирование из `account-packager`; встроенный в промпт чек-лист типичных признаков нейрослопа (канцелярит, шаблонные вступления/выводы, избыточные риторические вопросы, неестественно гладкая структура).

**Выход:** вердикт `ok` / `needs_revision` + список нарушений (цитата, категория, объяснение). Агент **не переписывает текст сам** — только критикует, чтобы не плодить ещё один источник искажений.

**Логика при `needs_revision`:**
1. Фидбек уходит обратно в `copywriter`/`reels-writer` как доп. инструкция — одна автоматическая перегенерация.
2. Если и повторная попытка не проходит — не зацикливаем. Контент уходит в UI с пометкой «требует ручной проверки» + список найденных проблем, в то же редактируемое текстовое поле, что используется для точечной правки (см. раздел 6).

---

## 6. Интерфейс пользователя (Front-end)

**Лендинг** (публичная страница) — описание, примеры, кнопка «Получить демо-доступ» → форма контакта (email + необязательное имя, см. раздел 2).

**Экран онбординга и загрузки:**
- Поля ввода ссылок на соцсети (Telegram, ВК).
- Блок ссылок на конкурентов.
- Форма-опросник.
- Кнопка запуска анализа с индикатором прогресса.

**Информационный дашборд (Кабинет эксперта):**
- Статус готовности профиля, распакованного метода и стиля (с честными статусами `боевой`/`черновик-*`, если данных не хватило).
- Рекомендации по оформлению профиля (от `account-packager`).

**Контент-хаб (двухнедельный план):**
- Календарь/таблица по дням и платформам — темы/заголовки на 14 дней, темы с открытой пометкой, если формат опирается на подтверждённый паттерн конкурентов.
- 3 демо-карточки в два шага: сначала текст поста / сценарий рилса (генерируется сразу); для рилса здесь же — необязательная загрузка референсов под уже готовый сценарий (см. Шаг 4); и кнопка «Сгенерировать» — после клика картинка или видео. Пометка, что это демонстрация качества, полный объём генерируется дальше по запросу.

**Панель экспорта (реализована в сужённом виде, решение сессии 2026-09-03):**
- «Копировать текст» в один клик — реализовано для постов (`copywriter`) в кабинете.
- Скачивание медиафайлов — картинки/видео уже скачивались, посты теперь тоже (.txt).
- ~~Редактируемое текстовое поле~~ — решение пользователя: не делаем, клиент копирует текст и правит его сам вне инструмента.

**Страница результатов** — отдельная read-only ссылка (см. раздел 2), для показа/пересылки готового демо без возможности повторного запуска пайплайна.

---

## 7. Инфраструктура и хранение данных

- **Сервер:** self-hosted, без внешних платных сервисов на MVP-этапе.
- **Файлы (референсы пользователя, сгенерированные картинки/видео):** хранятся на диске сервера, отдаются как статика через nginx/Caddy — даёт публичные HTTP(S)-URL (нужны для `generate_video`: `first_frame_url`, `reference_image`) без внешнего object storage (S3/R2). В БД хранятся только пути/URL, не сами файлы.
- **БД:** SQLite — метаданные клиентов, статусы, пути к файлам, **и данные распаковки ЦА/экспертности/графа знаний per-клиент** (см. раздел 3, "Данные клиента" — решение скорректировано после живого теста `server-memory`).
- **`@modelcontextprotocol/server-memory`:** подключён в `.mcp.json`, но только как память Claude Code между сессиями разработки — не продакшн-хранилище (см. раздел 3).
- **Медиа-генерация:** существующий `smm-mcp` сервер (`generate_image`/`generate_video` через OpenRouter, см. `smm-mcp/README.md`).
- **Текстовые LLM:** OpenRouter API, распределение моделей — см. раздел 4.

**Оценка занимаемого места (сервер 40GB):** Docker-образы (бэкенд, фронтенд, SQLite не требует отдельного контейнера, nginx, MCP медиа-генерации) — ориентировочно 1–2GB суммарно. Данные клиентов в SQLite — единицы МБ на MVP-масштабе. Демо-генерация на клиента (1 картинка TG + 1 картинка ВК + 1 видео-рилс) — ориентировочно до ~30MB. Итого для MVP — меньше 1GB данных сверх образов, с большим запасом.

**На будущее (не блокирует MVP):** при росте до полноценной генерации всего 2-недельного плана с медиа на каждый день для многих клиентов — хранилище видео станет расти существенно быстрее, тогда вопрос вернётся уже как вопрос масштабирования (возможно, тогда и имеет смысл вернуться к объектному хранилищу типа Cloudflare R2, благо у него есть щедрый бесплатный тариф).

---

## 8. Открытые вопросы на будущее (не блокируют MVP)

- **Лендинг с обучающим интерактивом** — идея озвучена, требует отдельной проработки контента/сценария.
- **Конкретный SMTP/email-провайдер для прода** — ещё не выбран (код-агностик, вопрос только в переменных `.env`).

**Решено, но не сделано (запланировано перед деплоем):** возврат `audience-unpacker`/`expertise-unpacker` на Claude Sonnet 5 — провизорное решение раздела 4 про DeepSeek V4 Pro подтвердило себя живым тестом (галлюцинированный tool-call у клиента «держим планку», см. раздел 4). Пользователь решила: откат делаем, но перед деплоем, не сейчас.

Закрыт (решение сессии 2026-09-03): **полный контент-план с медиа на все 14 дней.** Не следующая итерация — вне рамок проекта: делаем демо, дальнейших итераций не планируется.

Закрыт (решение сессии 2026-09-03): **масштабирование хранилища видео при росте числа клиентов.** Значимый прирост в рамках этого проекта не ожидается.

Закрыт (решение сессии 2026-09-03): **сбор Telegram/ВК клиента после демо.** Покрыто уже собираемыми email+именем (раздел 2) — отдельно собирать tg/vk не будем.

Закрыт (решение сессии 2026-09-03): **повторный тест `target_audience.md` на DeepSeek vs Claude.** Не нужен отдельным синтетическим прогоном — пользователь пройдёт всю цепочку живьём (лендинг → заявка → email → весь пайплайн до генерации видео на DeepSeek), как только будут готовы лендинг и SMTP; поведение Claude на этой паре агентов уже неоднократно проверено раньше, сравнивать по факту будет не с чем — решение по модели примет по результату полного живого прогона, не по отдельному контролируемому тесту.

Закрыт (решение сессии 2026-09-02, актуализировано 2026-09-03): **решение по референсам для картинок-постов.** Визуальный стиль для картинок синтезирует `account-packager` из текста (позиционирование/ЦА/метод), не `visual-style-analyzer` из фото клиента — см. раздел 9.

Закрыт (решение сессии 2026-09-03): **выбор канала связи email/Telegram/ВК на входе.** Заявка теперь принимает только email — Telegram/ВК как альтернативные каналы контакта сняты с повестки (не то же самое, что own-соцсети клиента для контент-анализа, там ничего не менялось), вместе с ними снят и вопрос об автодоставке ссылки через Telegram/ВК (deep-link для `chat_id`, VK Callback API) — он был актуален только при нескольких каналах на выбор. См. раздел 2.

Закрыт (решение сессии 2026-09-03): **`POST /api/agents/run-all` как синхронный HTTP-запрос.** Опасение было в риске таймаута браузера/прокси на ~8-минутном полном прогоне. На практике фронтенд `run-all` не вызывает вообще — кабинет получил кнопку «Запустить» на каждом этапе отдельно (раздел 9, «Текущее состояние фронтенда»), продуктового пути к `run-all` нет. Эндпоинт и `pipeline.ts` оставлены как есть для сквозных live-тестов при разработке; переделывать в фоновую задачу/SSE незачем без реального сценария использования.

---

## 9. Прогресс реализации и следующие шаги

*Раздел для непрерывности между сессиями разработки — обновлять по ходу работы, не только в конце.*

### Сделано и проверено живыми вызовами (не только сборкой/юнит-тестами)

- **Флоу доступа:** заявка с лендинга → уведомление оператору в Telegram с кнопками → ручное подтверждение (CLI или кнопка) → одноразовая magic-ссылка → обмен на httpOnly-сессию.
- **Онбординг:** `POST/GET /api/onboarding` (свои соцсети, конкуренты, опросник). Загрузка референсов на онбординге (`POST /api/onboarding/references/:category`) убрана из фронтенда решением сессии 2026-09-02 — маршрут и таблица `reference_files` в коде остались нетронуты (ими всё ещё пользуются `visual-style-analyzer`/`reels-writer`), но фронтенд больше в них ничего не пишет; судьба этой связки для картинок — отдельный вопрос, пользователь отложила его на потом.
- **`audience-unpacker` подключён к OpenRouter** (`anthropic/claude-sonnet-5`): собирает контекст из данных онбординга, вызывает модель, парсит YAML-frontmatter результата, сохраняет версионно (append-only, повторный запуск не перезаписывает предыдущую версию молча — см. автономный режим в `prompts/target-audience.md`).
- Живой тест на намеренно скудных тестовых данных подтвердил: агент в автономном режиме реально уходит в честный hard-stop (Фаза 0, ветка D промпта) вместо того чтобы выдумать сегментацию — автономная адаптация промпта держится не только на бумаге.
- **`expertise-unpacker` подключён тем же паттерном** (`anthropic/claude-sonnet-5`), с последней версией `audience_profiles` клиента как фоновым контекстом («для кого метод», не цель этой задачи — см. `prompts/expertise.md`, Шаг 0.3). Новая таблица `expertise_profiles` (append-only по версиям, статусы только `боевой`/`черновик-рамка` — без skeleton-варианта, которого нет в этом промпте). Живой тест на тех же скудных тестовых данных подтвердил тот же честный hard-stop.
- Публичный репозиторий на GitHub (`nanosvetik/ai-smm-assistant`), git-workflow — коммит и пуш на каждом завершённом шаге.
- **Парсер соцсетей готов для обеих платформ** (`backend/src/parsers/`): `telegram.ts` (публичная `t.me/s/<channel>`, HTML через `cheerio`), `vk.ts` (`wall.get`, `VK_SERVICE_TOKEN`) — единая точка входа `fetchPosts(platform, url)`, тип `ParsedPost` включает `engagement` (просмотры на обеих площадках; лайки/репосты только VK — публичное превью Telegram их не показывает). Проверено живыми вызовами на публичных каналах.
- **`account-analyzer` подключён к OpenRouter** (`deepseek/deepseek-v4-flash` — не Claude: задача рутинная, эмпирическое описание фактического стиля без сложных мета-инструкций, см. обоснование распределения моделей в разделе 4). Берёт посты по `own`-ссылкам клиента через `fetchPosts`, вызывает модель по новому промпту `prompts/account-analyzer.md` (написан с нуля, готового черновика от заказчика не было), сохраняет версионно в `account_style_profiles` (append-only, как и оба unpacker'а). Статусы только `боевой`/`черновик-скелет` — нет промежуточного «есть публичные источники, но не свои данные» варианта, как у unpacker-агентов, потому что вход здесь всегда либо реальные посты эксперта в достаточном объёме, либо их не хватает. Живой тест на реальном канале+паблике: 38 постов, статус `боевой`, содержательный разбор с дословными цитатами оборотов и хуков.
- **`competitor-analyzer` подключён тем же паттерном** (`deepseek/deepseek-v4-flash`). Берёт `competitor`-ссылки клиента, тянет посты через `fetchPosts`, ранжирует по вовлечённости (Telegram — просмотры; VK — просмотры + лайки×20 + репосты×50, простая эвристика: репосты весят больше как самый активный сигнал вовлечённости) и передаёт модели топ-5 постов на конкурента с реальными цифрами. Промпт `prompts/competitor-analyzer.md` написан с нуля, ищет кросс-паттерны — темы/форматы, подтверждённые у 2+ конкурентов независимо (кандидаты на пометку «формат подтверждён в нише» для будущего `content-planner`), отдельно от единичных сигналов у одного конкурента. Статус `боевой` требует минимум 2 конкурентов с 3+ постами каждый — код перепроверяет этот порог сам, не полагаясь только на самооценку модели в frontmatter. Результат — append-only в `competitor_analysis_profiles`. Живой тест на двух реальных каналах: статус `боевой`, найден и обоснован цифрами один кросс-паттерн («анонсы новых возможностей для разработчиков» — высокая вовлечённость в обоих источниках).
- **`account-packager` подключён** (`deepseek/deepseek-v4-flash`) — синтезирующий агент поверх *последних версий* трёх готовых документов (`audience_profiles`, `expertise_profiles`, `account_style_profiles`), без нового анализа. Промпт `prompts/account-packager.md` написан с нуля: короткое позиционирование, единый tone of voice, готовый текст био (короткая/расширенная версия), персонализированные рекомендации по шапке/контактам для «Аудита профиля» (раздел 3, Шаг 3) — явно без статичного чек-листа общих советов. Итоговый статус — не выбор модели, а самый слабый статус из трёх входов, вычисляется кодом (`weakestStatus`) и подставляется обратно в YAML-frontmatter сохранённого документа, чтобы текст не расходился с колонкой в БД. Версии всех трёх входов зафиксированы в новой таблице `packaging_profiles` для трассировки. Живой сквозной тест всей цепочки (`audience-unpacker` → `expertise-unpacker` → `account-analyzer` → `account-packager`) на новом тестовом клиенте прошёл до конца.
- **`profile-header-analyzer` добавлен** (Claude Sonnet 5, новый агент сверх исходной таблицы раздела 4) — по итогам обсуждения с пользователем: раз в проекте уже есть vision-анализ, логично применить его и к реальной шапке профиля клиента, не только к загруженным референсам. Смотрит на настоящий аватар/обложку/описание (не референсы, не посты) и даёт чисто фактологическое описание, без оценок и рекомендаций. Парсер расширен под это: `vk.ts` — `fetchVkProfileHeader` через `groups.getById` (метод, а не `groups.get` — этот сначала перепутали, он возвращает сообщества пользователя, а не данные сообщества по id/домену); `telegram.ts` — `fetchTelegramProfileHeader` из HTML `t.me/s/<channel>` (классы `.tgme_page_photo_image img`, `.tgme_channel_info_description`, `.tgme_channel_info_header_title` — проверены curl'ом перед реализацией, не угаданы). Изображения в OpenRouter идут прямым публичным URL (не `data:` base64, как у `visual-style-analyzer`) — аватар/обложка и так публичны, скачивать не нужно. Статус чисто механический: `боевой`, если хотя бы по одной площадке получилось скачать аватар. Результат — append-only в новой таблице `profile_header_profiles`.
- **`account-packager` получил опциональный четвёртый вход** — `Аудит шапки профиля` от `profile-header-analyzer`. Если есть — Шаг 4 промпта («Рекомендации по шапке и контактам») строится как сравнение факта с тем, что должно быть исходя из ЦА/метода, а не абстрактные советы. Если нет — работает по-старому, с явной оговоркой в тексте, что реальный профиль не анализировался. Это дополнительный факт для одного раздела, не показатель качества всего документа — его отсутствие не в `PrerequisitesMissingError` и не влияет на итоговый статус упаковки; версия сохраняется в `packaging_profiles.profileHeaderProfileVersion` (`null`, если не использовался).
- **Живой тест: механика доказана, «путь совпадения» — нет.** `profile-header-analyzer` дал точный, детальный фактологический разбор (вплоть до дословных цитат текста описания и палитры обложки) на реальных `t.me/durov` + `vk.com/apiclub` — тех же тестовых аккаунтах, что использовались весь сеанс вместо настоящих ссылок клиента. Но `account-packager` на Шаге 4 отказался сравнивать аудит с рекомендациями: честно написал, что «присланные ссылки не являются каналами эксперта» (вымышленный клиент-психолог из тестовых данных не имеет отношения ни к Дурову, ни к VK Mini Apps), и вернулся к рекомендациям без сверки. Разумная защита от натягивания несвязанных фактов, не баг — но означает, что путь «аудит реально про того же клиента» (как в проде) живым тестом не проверен; решили не гонять ради этого ещё один дорогой vision-тест, само проверится на первом реальном клиенте.
- **Попутно исправлен баг в `parseFrontmatter`** (`backend/src/lib/frontmatter.ts`), общий для всех агентов: модель иногда вставляет разделитель `---` в тело документа до настоящего YAML-frontmatter, из-за чего первая non-greedy пара `---...---` захватывала не тот кусок текста и валила YAML-парсер, роняя весь запуск агента без содержательной причины в логе. Обнаружено при первом сквозном прогоне всей цепочки на `account-packager`. Исправлено — теперь перебираются все пары строк `---` по порядку, берётся первая, которая реально парсится как YAML-объект.
- **`content-planner` подключён тем же паттерном синтеза** (`deepseek/deepseek-v4-flash`) поверх *последних версий* `packaging_profiles` и `competitor_analysis_profiles` (не сырые ЦА/экспертность/стиль заново, они уже сжаты `account-packager`), плюс реальные own-площадки клиента из `social_links`. Промпт `prompts/content-planner.md`: план тем и заголовков на 14 дней **только по тем площадкам, что реально есть у клиента** — если своих ссылок только на Telegram (форма онбординга разрешает 0–2 own-ссылки, без минимума в одну площадку), плана и идей Reels для ВК в документе не будет вообще (Reels в продукте существуют только для ВК); если только ВК — план и Reels под ВК без Telegram. Пометка `[формат подтверждён в нише: ...]` — где тема реально адаптирует кросс-паттерн конкурентов, а не копирует чужой текст. Только темы/заголовки — без полных текстов постов и без выбора конкретных демо-позиций (это остаётся за `copywriter`/`reels-writer` дальше по пайплайну). Статус — тем же способом, что и `account-packager`: самый слабый из двух входов, вычисляет код. Результат — append-only в `content_plans`, с реальными площадками (`platforms`) и версиями обоих входов для трассировки.
- **Рефакторинг `replaceFrontmatterField`:** локальный `patchStatus` в `account-packager` использовал ту же хрупкую regex-логику для поиска блока `---`, которую только что чинили в `parseFrontmatter`, — тот же класс бага рисковал повториться при копировании кода в `content-planner`. Вынесено в общую `replaceFrontmatterField(document, field, value)` в `backend/src/lib/frontmatter.ts` поверх той же устойчивой логики перебора пар `---`.
- **`copywriter` подключён** (`deepseek/deepseek-v4-flash`) поверх *последних версий* `content_plans` + `packaging_profiles`, для конкретной площадки и дня плана (параметры запроса, `day` по умолчанию 1). Извлечение темы дня из markdown-таблицы плана не парсится кодом структурно — модель сама находит нужную строку по явно переданным площадке/дню и использует тему дословно, тем же принципом, что и во всех остальных агентах (передаём весь документ, не структурированные поля). Промпт `prompts/copywriter.md` написан с нуля, с явным правилом «честность живёт в метаданных, не в тексте поста»: пост — клиентский текст, статус `черновик-*` остаётся только в YAML-frontmatter, не просачивается оговорками в сам текст. Каждая площадка (`telegram`/`vk`) версионируется независимо в новой таблице `copywriter_posts` — можно перегенерировать одну площадку, не трогая другую. Статус — тем же способом, что и у `account-packager`/`content-planner`: самый слабый из двух входов. Проверяет запрошенную площадку против `content_plans.platforms` — `PlatformNotInPlanError`, если у клиента этой площадки нет, до вызова модели.
- Живой сквозной тест всей цепочки (`audience-unpacker` → `expertise-unpacker` → `account-analyzer` → `competitor-analyzer` → `account-packager` → `content-planner` → `copywriter` ×2) на новом тестовом клиенте прошёл до конца: оба поста (Telegram и ВК) получились содержательными, по теме дня из плана, без нейрослоп-клише, с честной пометкой `черновик-скелет` только в метаданных (не в тексте) и с адаптацией длины/тона под площадку (короче и рубленее в Telegram, развёрнутее в ВК — согласуется с находками `account-analyzer` по этим же каналам).

- **`visual-style-analyzer` подключён** (Claude Sonnet 5 — не DeepSeek, новый агент сверх исходной таблицы раздела 4, см. «Архитектурное решение этой сессии» ниже). Разово анализирует drag-and-drop референсы клиента (не посты) vision-моделью: палитра, свет, композиция, чего в референсах нет, — и выдаёт практичный дескриптор для промптов `generate_image`, чтобы будущий `visual-generator` не выдавал стилистически разрозненные картинки от раза к разу. `lib/openrouter.ts` расширен под мультимодальные сообщения (`content` может быть массивом text/image_url блоков, картинки — как `data:` base64 URI, без публичного хостинга). Изначально пробовали `deepseek/deepseek-v4-flash-vision-exp` — заблокировано privacy/data-policy настройками аккаунта OpenRouter (`404 No endpoints available matching your guardrail restrictions and data policy`, требуется включить разрешение на обучение на входных данных ради дешёвой экспериментальной модели). Решили не включать: референсы вроде «до/после» — потенциально личные фото, не публичный контент, и это разовая операция на клиента, не на каждый пост — тот же аргумент, по которому Claude выбран для `audience-unpacker`/`expertise-unpacker` (раздел 4). Переключили на Claude Sonnet 5, живой вызов прошёл.
- **Статусный порог — пол, не потолок.** Первый живой тест (три намеренно разнородных фото — разные палитра/свет/композиция) вскрыл баг: код ставил `статус: боевой` по правилу «3+ референса», не читая, согласован ли стиль по мнению модели — хотя сама модель в теле документа честно написала, что это не боевой профиль. Исправлено по образцу `competitor-analyzer`: число референсов ниже порога принудительно даёт `черновик-скелет`, но выше порога код доверяет качественной оценке модели, а не автоматически завышает до `боевой`. Промпт (`prompts/visual-style-analyzer.md`) дополнен явной само-проверкой: frontmatter не может утверждать `боевой`, если тело документа описывает противоречие между референсами. Повторный живой тест на тех же трёх фото подтвердил: модель честно поставила `черновик-скелет`, согласованно в frontmatter и тексте.
- **`visual-generator` подключён** (`deepseek/deepseek-v4-flash` — вход тут уже текст, не изображения, vision не нужен). Берёт *последний* пост `copywriter` для указанной площадки и, если есть, *последний* `Визуальный style-профиль`, пишет промпт для `generate_image` — сам вызов `generate_image` не делает, это отдельный дорогой шаг за гейтом подтверждения пользователя (раздел 3, Шаг 4 — гейта в коде ещё нет). Промпт `prompts/visual-generator.md` написан с нуля: сюжет иллюстрации от темы поста (не буквальная калька), явный запрет на стоковые клише и типичные признаки нейрогенерации, готовый промпт — на английском (точнее работает с Seedream/FLUX.2), краткое русское описание — для превью в интерфейсе перед кнопкой «Сгенерировать». **Открытый вопрос из этого же раздела про отсутствие референсов решён здесь:** если `Визуального style-профиля` нет (клиент не грузил референсы), агент не блокируется — пишет по нейтральному нейрослоп-стойкому дефолту и честно помечает `визуальный_профиль_использован: false`, а не требует профиль как жёсткую предпосылку. Статус — самый слабый из поста и профиля (если профиль есть), тем же способом, что у `account-packager`/`content-planner`/`copywriter`. Каждая площадка версионируется независимо в `visual_generator_prompts`. Живой тест прошёл в обоих режимах (с профилем и без) на одном тестовом клиенте — в обоих случаях получился связный, конкретный промпт без клише.
- **Клиент с одной площадкой — вопрос пользователя, не находка теста.** Форма онбординга и так разрешала 0–2 own-ссылки (`.max(2)` без `.min()`), но `content-planner` был написан с жёстким предположением «у клиента есть и Telegram, и ВК» — план всегда включал обе колонки и Reels-идеи для ВК, даже если own-ссылка у клиента только одна. Решение (обсуждалось с пользователем): план и демо строятся только под реальные площадки клиента, не «на будущее» под площадку, которой нет. Реализовано: платформы читаются из `social_links` (role=own), не из `account_style_profiles.platforms` (там площадка могла выпасть, если парсер не нашёл постов, хотя клиент её указал); `content_plans` хранит колонку `platforms`; `copywriter` проверяет запрошенную площадку против неё и падает `PlatformNotInPlanError`, если её там нет. Живой тест на клиенте только с Telegram: план получился из одной колонки без раздела ВК/Reels и без упоминания второй площадки даже в финальной сноске; `copywriter` для Telegram прошёл штатно, для ВК — корректно отклонён до вызова модели.
- **Оркестрация: `POST /api/agents/run-all`** (`backend/src/agents/pipeline.ts`, по итогам обсуждения с пользователем — до этого все агенты вызывались строго по одному, только тестовыми скриптами). Независимые агенты идут параллельно через `Promise.all`: `audience-unpacker`→`expertise-unpacker` одной последовательной веткой (экспертность точнее с готовым Профилем ЦА в фоне) плюс параллельно ей `account-analyzer`, `competitor-analyzer`, `visual-style-analyzer`, `profile-header-analyzer` — 5 веток вместо 6 последовательных вызовов. Дальше — жёсткая цепочка по зависимостям в БД (`account-packager` → `content-planner` → `copywriter`+`visual-generator`), но `copywriter`/`visual-generator` для разных реальных площадок клиента тоже выполняются параллельно между собой. `visual-style-analyzer` и `profile-header-analyzer` — необязательные ветки, их падение не останавливает пайплайн (`runOptionalStage`); `audience`/`expertise`/`account-analyzer`/`competitor-analyzer` — обязательные, останавливают честно (`runStage`). Ответ всегда `200` с отчётом `ok`/`failed`/`skipped` по каждому этапу — то, что какой-то агент не прошёл по домену (нет референсов, нет предпосылок), предметная информация для фронтенда, не серверная ошибка. Оркестратор не хранит состояния — каждый этап пишет в БД сам, `run-all` можно звать заново после исправления причины сбоя. Живой тест: полный прогон (10 реальных этапов + 1 корректно пропущенный из-за отсутствия референсов) — **459 секунд**, против 15–20+ минут у последовательных прогонов раньше в этой сессии.
- **Открытый вопрос:** ~8 минут — многовато для обычного синхронного HTTP-запроса (браузер/прокси часто рвут соединение на 60–120 сек). Для реального фронтенда `run-all`, вероятно, придётся стать фоновой задачей с отдельной ручкой статуса (поллинг) или получить SSE/WebSocket-прогресс — решать вместе с фронтендом, не сейчас.
- **`reels-writer` подключён** (DeepSeek V4 Pro — см. сноску к таблице раздела 4) поверх *последних версий* `content_plans` + `packaging_profiles`. Reels в продукте существуют только для ВК (см. `content-planner` выше) — без ВК в реальных площадках клиента агент падает `ReelsNotAvailableError` до вызова модели; сценарий один на клиента, без разбивки по площадкам, в отличие от `copywriter_posts`/`visual_generator_prompts`. Промпт `prompts/reels-writer.md` (написан с нуля): модель сама выбирает одну идею из раздела «Идеи Reels» контент-плана — предпочитает идею, реально покрытую загруженными референсами клиента (категории референсов передаются текстом, без vision — агент не смотрит на сами фото), иначе нейтральный формат без опоры на личные материалы, с явным запретом выдумывать несуществующие референсы. `использованы_референсы`/`категории_референсов` — решение модели из frontmatter, код не назначает их сам (тот же принцип, что `b2b`/`segments` в `audience-unpacker`). Результат — append-only в новой таблице `reels_scripts`. Живой тест на реальном клиенте (метод «замечать момент проседания энергии в течение дня») дал содержательный сценарий: хук, семишотовая раскадровка, текст озвучки — ровно по методу клиента, без нейрослоп-клише.
- **`editor-in-chief` подключён** (DeepSeek V4 Pro) — реализует раздел 5 целиком. Вход: проверяемый текст (последняя версия `copywriter_posts` или `reels_scripts`) + табу с обоснованиями (`expertise_profiles`) + стоп-слова/tone of voice сегмента (`audience_profiles`) + tone of voice/позиционирование (`packaging_profiles`) — все четыре обязательны, `PrerequisitesMissingError` иначе. Промпт `prompts/editor-in-chief.md`: агент не переписывает текст сам, только вердикт `ok`/`needs_revision` + список нарушений (категория/цитата/объяснение) в теле документа — ровно так, как зафиксировано в разделе 5. Нераспознанный вердикт из frontmatter код трактует как `needs_revision`, не `ok` (ложноположительный `needs_revision` дешевле, чем пропущенное нарушение у живого пользователя). Результат — append-only в новой таблице `editorial_reviews`, версия отдельно на пару (`contentType`, `platform`). Живой тест на намеренно табу-нарушающем тексте («у вас клиническая депрессия, начните принимать антидепрессанты») подтвердил: агент реально ловит нарушения с точными цитатами (два табу + драматизация тона), не штампует `ok` по умолчанию.
- **Retry-логика раздела 5 реализована отдельным оркестратором**, не внутри самих генераторов — решение этой сессии (обсуждалось с пользователем): `backend/src/agents/reviewedContent.ts` (`runReviewedCopywriter`/`runReviewedReelsWriter`) держит цикл генерация → проверка → при `needs_revision` одна автоматическая перегенерация с вердиктом редактора как доп. инструкцией (новый параметр `editorFeedback` в `runCopywriter`/`runReelsWriter`) → повторная проверка; дальше не зацикливается, возвращает `needsManualReview: true`. `copywriter.ts`/`reelsWriter.ts` остаются чистыми генераторами, каждый уже проверен живым вызовом сам по себе — оркестрация, как и везде в проекте, живёт отдельно от промпта и от самого агента. Живой тест подтвердил не только happy path: на намеренно плохом входе первая перегенерация ушла от табу-нарушения, но `editor-in-chief` поймал на второй итерации уже более тонкие проблемы (драматизация тона, шаблонная концовка-вопрос) — вторая проверка не формальность, а реальный фильтр, ради которого и нужен `needsManualReview`.
- **`run-all` дополнен**: `copywriter`/`reels-writer` в пайплайне теперь идут через `runReviewedCopywriter`/`runReviewedReelsWriter`, то есть `run-all` с этого момента отдаёт уже отревьюженный контент, не сырой; `visual-generator` для площадки стартует уже после её editor-in-chief-проверки (в т.ч. после retry), так как берёт «последнюю версию» поста из БД. `reels-writer` — параллельно с `copywriter`, но только если у клиента есть ВК, иначе `skipped`, не `failed`. Живой тест на клиенте с обеими площадками прошёл весь пайплайн до конца; в одном прогоне `reels-writer` упал по транзиентной сетевой ошибке OpenRouter — при повторном отдельном вызове тут же отработал штатно, не баг кода (см. `runStage` — падение одной ветки не блокирует остальной пайплайн).
- **Фронтенд онбординга собран** (`frontend/` — React 18 + React Router 6 + Vite 5 + TypeScript, пиновки версий вручную вместо дефолта `npm create vite@latest`, см. «Известные грабли» в CLAUDE.md). **Архитектурное решение сессии: magic-ссылка ведёт на фронтенд, не на бэкенд.** Было `{BASE_URL}/api/access/:token` — тупик для живого пользователя (голый JSON, не HTML); стало `{BASE_URL}/onboarding/:token` (`backend/src/admin/approval.ts`) — экран `TokenExchangeScreen` сам вызывает обмен ссылки из браузера и либо показывает форму, либо честную ошибку (использована/просрочена/не найдена) в стиле дизайн-системы. `BASE_URL` по умолчанию теперь смотрит на дев-порт фронтенда, не бэкенда — в проде без разницы, один домен на оба сервиса (раздел 7). Vite dev-прокси `/api` → бэкенд повторяет прод-топологию (nginx/Caddy под одним доменом), cookie-сессия работает без CORS-конфигурации на обеих. `OnboardingScreen` реализует экран из Брифа 2 design-brief-ателье.md: короткие поля (модель продаж, свои/конкурентные ссылки) + карточка-интервью с 4 открытыми вопросами (сигнатурный элемент — аватар-метка «„», не дженерик-иконка) + drag-and-drop по 5 категориям референсов (грузятся сразу при дропе). Кнопка «Запустить анализ» намеренно не входит в этот экран — решение сессии, отдельно от нерешённого вопроса про 8-минутный `run-all` выше. Типографика — **Alegreya + Golos Text**, не Fraunces/Figtree из самого брифа (у обеих нет кириллицы, проверено через Google Fonts metadata API до выбора — см. «Известные грабли»), самохостятся `.woff2`, без Google Fonts CDN в рантайме. Живой тест: полный флоу через реальный `fetch` (обмен ссылки → сессия → сохранение анкеты с кириллицей → загрузка референса → повторное использование ссылки корректно даёт `link_already_used`), внешний вид визуально подтверждён пользователем в браузере.
- **Кабинет эксперта собран** (`/dashboard`, `DashboardScreen.tsx`) — сайдбар ровно на 9 пунктов из Брифа 2 (решение сессии, обсуждалось с пользователем): не 1-в-1 с 12 агентами раздела 4, `visual-style-analyzer`/`profile-header-analyzer` без своего пункта (вспомогательные входы), `editor-in-chief` невидим (встроен в `copywriter`/`reels-writer`, см. выше). Пройденные этапы — изумрудная галочка, текущий — сливовая подсветка, впереди — серый (Бриф 2, `Sidebar.tsx`). **Решение сессии про запуск: раз `run-all` отложен (8-минутный таймаут), у каждого этапа своя кнопка «Запустить»/«Переделать»** (`StagePanel.tsx`) поверх соответствующей `POST /api/agents/<slug>` — пользователь реально проходит конвейер по шагам из UI уже сейчас, не дожидаясь решения по фоновым задачам/SSE для `run-all`. Документ агента рендерится через `react-markdown`+`remark-gfm`, frontmatter скрыт от клиента (`stripFrontmatter`) — статус и так показан отдельно из колонки БД.
- **Три живых бага найдены и исправлены в процессе показа кабинета пользователю** (не находки заранее продуманных тест-кейсов — реальные баг-репорты в моменте): (1) `isDone()` использовал `platforms.every(...)` без проверки на пустой массив — `[].every(...)` вакуумно даёт `true`, из-за чего площадочные этапы («Посты», «Изображения») показывали галочку «пройдено» без единого реального запуска, если у клиента (или протухшей сессии в браузере) не было площадок; (2) фронтенд показывал клиенту сырой YAML-frontmatter, потому что `stripFrontmatter` проверял только начало документа — `audience-unpacker` в pre-baseline режиме кладёт преамбулу и decoy-`---` перед настоящим frontmatter внутри ```` ```yaml ```` кодфенса (тот же класс бага, что уже чинили в бэкенде, см. `backend/src/lib/frontmatter.ts`), исправлено переносом той же устойчивой логики на фронтенд (`js-yaml` добавлен и туда); (3) `react-markdown` без `remark-gfm` не рисовал markdown-таблицы (только базовый CommonMark из коробки) — добавлен плагин.
- **Живой прод-баг найден через кабинет, не через тест-скрипт: `target-audience.md`/`expertise.md` добавляли отсебятину перед документом.** Пользователь заметил в реальном выводе `audience-unpacker` отдельное вступление «прежде чем выдать документ, важные технические замечания...» с пересказом внутренней кухни промпта («тест не сработал бы, если бы я получила...») — промпт такой преамбулы не требует (см. «Запись результата» — тело документа начинается сразу с `# Профиль ЦА`), а честность о статусе и пробелах и так устроена через `статус:` frontmatter, явную плашку и метки источника `[гипотеза по нише]`/`[требует уточнения у эксперта]`. Тот же принцип «честность в метаданных, не в отдельном тексте», что уже стоит в `copywriter.md` для постов. Исправлено явным запретом в обоих промптах. Живой повторный тест на том же клиенте подтвердил: документ версии 2 начинается сразу с frontmatter, без преамбулы.
- **Более серьёзный баг в той же паре промптов, найден в следующей сессии тем же способом (чтение сохранённого документа в БД, не тест-скрипт): `expertise.md` не имел форс-автономного режима, который уже стоял в `target-audience.md`.** Кнопочный запуск этапов вместо `run-all` (решение прошлой сессии) означал, что через кабинет реально проверяли только `audience-unpacker` — `expertise-unpacker` после перехода на кнопки живьём не гоняли. Сохранённый в БД документ оказался не распаковкой экспертности, а моделью, застрявшей на первом вопросе интервью: `expertise.md` (Шаг 0.5), в отличие от `target-audience.md` (Шаг 0.4), не содержал строки «для этого продукта режим всегда автономный» — модель сама выбрала интерактивный режим, хотя продукт делает один запрос без чата и продолжить диалог не может. Код не ловил это: `parseFrontmatter` не находит YAML в таком ответе, тихо возвращает `null`, `expertiseUnpacker.ts` подставляет дефолтный статус `черновик-рамка` и сохраняет обрывок интервью как финальный документ клиента — без валидации, что документ вообще собрался. Исправлено переносом той же формулировки форс-автономного режима в `expertise.md`; ветки A/Б (эксперт в диалоге) оставлены в тексте промпта как недостижимые для этого продукта, но не удалены — промпт остаётся самостоятельным документом, который эксперт может подгрузить и в нейросеть с живым чатом (см. раздел 4, «Структура кода агента»). Живой повторный тест подтвердил: документ дособирается до конца со статусом `черновик-рамка`.
- **По прямому запросу пользователя после прочтения обоих реальных документов — тон почищен от утечки внутренней механики промпта и стал проще.** Чтение сохранённых документов целиком (не только факт успешного запуска) показало, что даже при успешном прогоне оба агента писали «слишком технично»: прямые ссылки на номера шагов/фаз промпта в тексте («диагностика по Шагу 1.3», «по пустым пунктам Фазы 3»), название маркетингового фреймворка без объяснения (заголовок раздела `### Уровень осознанности по Ханту` — модель копировала «по Ханту» в поле вывода, хотя сама инструкция просила писать только «Уровень осознанности»), и остаточный decoy-`---` перед ```` ```yaml ```` кодфенсом, который фронтендовый `stripFrontmatter` не срезает (чистит только вплотную к фенсу, не до отдельно стоящего разделителя перед ним). Исправлено во всех трёх местах в обоих промптах, плюс добавлен новый принцип **«Простой человеческий язык»** в «Жёсткие принципы» — ориентир на читателя без профильного образования, явно не распространяется на метки источника и структуру документа (решение пользователя — оставить их как есть, они нужны без изменений для передачи вниз по пайплайну в `account-packager`/`content-planner`/`editor-in-chief`). Живой повторный тест подтвердил заметно более тёплый тон при сохранении статуса/меток/структуры без изменений.
- **Онбординг дособран визуально и текстово — по прямому запросу пользователя: экран читался «слишком спартански» для ЦА с развитым чувством вкуса (design-brief-ателье.md), без пояснений было непонятно, зачем каждый блок первому пользователю инструмента.** Добавлено: ориентирующая строка в шапке с итогом («дальше мы соберём портрет вашей аудитории, метод и черновик первых постов»); ненумерованная строка-«план страницы» (курсив Alegreya, сливовые слова-метки — сознательно без цифр 1/2/3, это одна форма, а не визард с зависимостями между шагами, нумерация была бы враньём о структуре); курсивные сливовые эйбраузы-заголовки над каждым смысловым блоком; пояснение под тогглом B2C/B2B (единственное поле без `hint`, хотя у `LinksField` он уже был). Сигнатурный элемент — крупная „ рядом с заголовком, тот же мотив, что уже был в аватаре `InterviewCard`. Живой баг найден пользователем сразу: первая версия ставила кавычку абсолютным позиционированием позади текста, глиф наезжал на буквы заголовка — исправлено на обычный `flex` рядом с текстом, без `position: absolute`.
- **Индикатор ожидания на кнопке «Запустить».** Живые вызовы занимают 1–3 минуты, раньше единственной обратной связью была смена текста кнопки — непонятно, зависло или работает. Добавлена бегущая полоска (CSS-анимация, не реальный процент) и строка «Работаем над документом — обычно 1–3 минуты. Не закрывайте вкладку», пустой стейт скрывается на время запуска.
- **Живой баг: новое обязательное поле анкеты (`expertPath`) тихо не сохранялось у существующих клиентов.** Форма при загрузке подставляла ответ сервера в состояние целиком — у клиентов, заведённых до появления поля, сервер честно возвращает `null`. При сабмите `.trim()` на `null` кидал необработанное исключение вне `try/catch` — форма молча ничего не отправляла, без ошибки на экране, пользователь решал, что сохранилось. Исправлено: анкета при загрузке докладывается дефолтами, нулable-поля коалесятся к `""` явно (просто спред не спасает — ключ с явным `null` перекрывает дефолт). Второй слой защиты — `expertiseUnpacker.ts` не подставляет `null` в шаблон напрямую (буквальный `${null}` в JS-темплейте рендерится как строка «null», что пользователь и увидел в документе).
- **По прямому запросу пользователя после прочтения реального демо-документа — добавлена рамка ожиданий для тех, кто просто «пришёл потыкать MVP».** Честный `черновик-рамка` с `требует уточнения у эксперта` — корректное поведение при 5 полях анкеты вместо интервью, но без контекста читается как «сломано». Не размазали по каждому экрану кабинета (бриф просит там скорость, не убеждение) — сказали один раз на экране «Данные сохранены» после анкеты, и коротко напомнили в момент реальной растерянности — рядом со статус-строкой при `черновик-*`: «Черновик — это честность инструмента при неполных данных, не ошибка». Отдельная защита от повторных дорогих Sonnet-вызовов из-за путаницы — решили пока не нужна, доступ закрыт ручным подтверждением заявки.
- **Точечное решение для orchestration-дыры `profile-header-analyzer`: спарили с кнопкой «Анализ аккаунта».** У обоих вход — только own-ссылки, оба независимы от остальных этапов — тематически логичная пара (реальный текстовый стиль + реальная шапка профиля), предложено пользователем вместо ожидания общего решения по `run-all`. `POST /api/agents/account-analyzer` теперь дёргает `profile-header-analyzer` через `Promise.all` на каждый клик, включая «Переделать» (кнопку `account-analyzer` всё равно уберут перед деплоем — см. решение про честную распаковку выше, поэтому не усложняли логикой «только при первом запуске»). Ошибка вспомогательного агента не роняет основной ответ. На фронтенде — `StageConfig.secondaryAgentSlug`, `DashboardScreen.tsx` подгружает его результат отдельно, `StagePanel.tsx` рисует вторым read-only блоком без своей кнопки. Живой сквозной тест подтвердил: `account-packager` реально видит новый вход (`profileHeaderProfileVersion` стало `1` вместо `null`).
- **Кабинет эксперта — правка легче, чем у онбординга: бриф прямо разводит два экрана** («здесь важна скорость и ясность, а не убеждение», Бриф 2). У каждого этапа (`lib/stages.ts`, поле `description`) — одна строка простым языком о том, что этап делает, курсивом под заголовком (тот же приём, что эйбраузы онбординга — сквозной стиль между экранами продукта). Пустой стейт этапа переформулирован так, чтобы вести к кнопке «Запустить», а не сухо констатировать факт. Сайдбар — три смысловых блока (разбор → стратегия → готовый контент) отделены увеличенным отступом, без заголовков и рамок групп — бриф здесь против лишней структуры. Галочки/цвета/подсветка не тронуты — уже 1-в-1 по Брифу 2.
- **Адаптивность: кабинет эксперта не переверстывался на узких экранах, найдено пользователем через devtools.** `DashboardScreen.css` держал сайдбар и панель бок о бок фиксированной шириной 240px без единого брейкпоинта — на ширине телефона не превращалось в колонку, а просто сжималось. Онбординг (одна колонка, `max-width: 640px`) уже переносил сужение нормально. Добавлено: `@media (max-width: 760px)` — кабинет в `flex-direction: column`, сайдбар на `width: 100%`; `@media (max-width: 420px)` — тоггл B2C/B2B в столбик (подстраховка). Подтверждено пользователем визуально в devtools на обеих точках.
- **Найдена и закрыта структурная дыра в онбординге: нигде не спрашивали, кто сам эксперт.** Разбор одного «не туда» ответа («Кто ваш клиент?» — эксперт описал себя, не аудиторию) вскрыл корень: во всём опроснике было только про клиента эксперта (`clientDescription`), метод (`mainPrinciple`) и табу — ни одного поля про самого эксперта. А Блок 2 «Экспертность и путь» в `expertise.md` (поворотные моменты, на чём построена экспертность) был структурно обречён оставаться пустым `требует уточнения у эксперта` у каждого клиента в автономном режиме, не только у тестового — данных для него неоткуда было взять. Формулировку подбирали две итерации с пользователем: отвергнутая версия смешивала три вопроса и резюме-язык («стаж»); финальная — один вопрос с тремя равноправными путями (учёба / практика / выросшее хобби), без слова «экспертность», проверена явно на кейсе «мастер с хендмейд-товаром», не только на дипломированном специалисте — то есть на реальной ЦА продукта (design-brief-ателье.md, «мастера, маленький бизнес»), не только на психологе из тестовых данных. Реализовано: колонка `expert_path` в `onboarding_profiles` (без `NOT NULL` — в таблице уже были тестовые строки без неё, обязательность на уровне zod-схемы и формы), пятый вопрос интервью-карточки, новая секция контекста в `expertiseUnpacker.ts` (`audienceUnpacker.ts` не тронут — Профилю ЦА эта информация не нужна). Живой повторный тест подтвердил фикс по существу: блок «Экспертный путь» заполнился реальным содержанием вместо пустых пометок.
- **Живой баг: та же утечка внутренней механики промпта, что чинили в `target-audience.md`/`expertise.md`, оказалась ещё в пяти промптах — на Sonnet не срабатывала, всплыла на DeepSeek.** В `account-analyzer` пользователь заметил заголовок `## 5. Финальное послесловие — следующий шаг`, а под ним тело начиналось с повторного `**Следующий шаг.**` — дубль. Причина одна во всех семи: инструкция буквально называла раздел «Финальное послесловие — следующий шаг» и тут же давала пример с ещё одним bold-лидом внутри цитаты — двусмысленно, выводить ли название пункта как заголовок буквально. Claude Sonnet (`target-audience.md`, `expertise.md`, `visual-style-analyzer.md`) сам упрощал до чистого `## Следующий шаг`; DeepSeek (`account-analyzer.md`, `account-packager.md`, `competitor-analyzer.md`, `content-planner.md`) воспроизвёл дословно — тот же класс провала на мета-инструкциях, что уже описан для DeepSeek в CLAUDE.md. Исправлено во всех семи: заголовок вывода назван явно, пример-цитата без своего bold-лида. Заодно в пять промптов без него добавлен запрет ссылаться на номера шагов в тексте документа (их структурные списки усеяны пометками `(Шаг N)`, не утекали пока, но были тем же риском). Живой повторный тест `account-analyzer` подтвердил: дубль ушёл.
- **Живой баг: DeepSeek приклеивал собственную оговорку к шаблонному тексту блока «Следующий шаг», без разделения.** В `competitor-analyzer` перед фиксированным текстом про передачу в `content-planner` модель дописала свою (справедливую) оговорку про нехватку данных по ВК-конкурентам одним абзацем без разделения — побочный эффект предыдущего фикса дубля заголовка (инструкция стала говорить «начинай сразу по существу», не уточняя, что новый анализ в этот блок вообще не должен попадать). Проверка `account-packager`/`content-planner` в этом же прогоне утечки не показала — не системный баг, ситуативный. Исправлено превентивно во всех семи промптах: явно «в блоке — только фиксированный текст, ничего от себя».
- **Живой баг (найден пользователем, не модельная утечка — недоработка в шаблонных текстах промптов): блок «Следующий шаг» называл внутренние имена агентов, а не документы.** Пять промптов ссылались в фиксированном тексте на `account-packager`/`content-planner`/`copywriter`/`visual-generator`/`reels-writer`/`generate_image` — слаги роутов и MCP-инструментов, бессмысленные для клиента, загрузившего документ в свою нейросеть. Там, где блок ссылался на документ (`Профиль ЦА`, `Упаковка профиля`) — было нормально. Исправлено на название следующего документа или обычное описание результата, без агент-слагов. Заодно добавлен ориентир по частоте Reels — `content-planner` инструктирован не привязывать идеи к дням, из-за чего документ не давал вообще никакого ориентира по частоте; добавлена честная гипотеза-строка (`[общая рекомендация, не основана на данных этого эксперта]`).
- **Плейсхолдеры полей приведены к дизайн-токенам вместо дефолта браузера.** Явного стиля `::placeholder` не было нигде — цвет зависел от браузера/ОС, не от палитры. Добавлено глобальное правило (`color: var(--color-muted)`, `opacity: 1` — перебивает дополнительное затемнение плейсхолдера по умолчанию в Firefox).
- **Контент-план: реалистичная частота публикаций + собственная сетка в кабинете вместо ежедневного плана (решение сессии 2026-09-01, по прямому запросу пользователя).** `content-planner` раньше требовал тему на каждый из 14 дней для каждой площадки — нереалистично для частного эксперта. `prompts/content-planner.md` (Шаг 2) теперь требует, чтобы модель сама выбрала реалистичную частоту по каждой площадке отдельно (честно помеченную как гипотеза-ориентир, тем же принципом, что уже стоял для периодичности Reels) и заполняла только эти дни. Модель дополнительно выдаёт машиночитаемый ```json-блок (`posts`/`reels`) сразу после frontmatter — парсится отдельно от прозы (`backend/src/lib/planData.ts`, тот же принцип перебора блоков, что и `parseFrontmatter`), сохраняется в новые nullable-колонки `content_plans.planItems`/`reelsIdeas` (миграция `0015_reflective_naoko.sql`). На фронтенде — новый компонент `ContentPlanGrid.tsx`, специально подключённый в `StagePanel.tsx` только для этапа `content-planner` (сетка по неделям/дням недели, столбцы только по реальным площадкам клиента, дни без поста не выводятся вообще; честный откат на обычный markdown-документ, если JSON не распарсился) + кнопка «Скачать таблицу (.xlsx)», генерирующая файл в браузере через `exceljs` (не `xlsx`/SheetJS — актуальная версия на npm registry несёт две известных high-severity уязвимости, патчи публикуются только на CDN SheetJS, не в npm). Живой тест на тестовом клиенте с обеими площадками подтвердил: модель выбрала «Telegram через день» / «ВК 2 раза в неделю», сетка и выгрузка отрендерились корректно (визуально подтверждено пользователем).
- **Закрыта дыра: editor-in-chief не проверял реальный контент из кабинета (решение сессии 2026-09-01, найдено пользователем при разборе живой генерации поста).** `POST /api/agents/copywriter`/`POST /api/agents/reels-writer` — единственный путь генерации из кабинета, пока `run-all` к UI не подключён (раздел 8) — вызывали генераторы напрямую (`runCopywriter`/`runReelsWriter`), минуя `runReviewedCopywriter`/`runReviewedReelsWriter` из `reviewedContent.ts`, которые были подключены только внутри `pipeline.ts`. То есть автопроверка табу/стоп-слов/нейрослопа (раздел 5) фактически не работала для реального пользователя кабинета. Исправлено — обе ручки теперь идут через reviewed-обёртку, ответ несёт поле `needsManualReview`. Фронтенд: предупреждение под статусом, если редактор не снял нарушения за две попытки; текст под прогресс-баром для этих двух этапов честно обновлён («2–5 минут» вместо «1–3» — теперь до 4 последовательных вызовов модели). Известное ограничение — `needsManualReview` не персистится, виден только сразу после запуска, не после перезагрузки страницы. Живой тест через реальную HTTP-ручку подтвердил обе ветки: первая генерация поймана как `needs_revision`, автоматическая перегенерация прошла проверку (`ok`).
- **Гейт подтверждения перед генерацией картинки реализован (решение сессии 2026-09-01) — только картинки, видео сознательно оставлено отдельной задачей.** См. раздел 8, пункт 2 — там резюме решения и причина, почему видео не в этой сессии. Технически: `backend/src/lib/imageGeneration.ts` — прямой вызов OpenRouter `/api/v1/images`, повторяет логику `generate_image` из `smm-mcp/src/index.js` (та же основная модель `bytedance-seed/seedream-4.5` + фолбэк `black-forest-labs/flux.2-max`, тот же каталог `workspace/06-images/`) — не через сам `smm-mcp` (MCP stdio-сервер для Claude Code, не HTTP-сервис для продакшн-бэкенда). `backend/src/lib/promptBlock.ts` (`extractPromptBlock`, изначально `imagePrompt.ts` — переименован в следующей сессии, тот же формат понадобился для видео-промпта) извлекает готовый промпт из документа `visual-generator` — промпт (`prompts/visual-generator.md`) доработан, чтобы оборачивать английский промпт в ` ```text ` блок для надёжного извлечения (тот же принцип, что JSON-блок `content-planner`). Новая таблица `generated_images` (append-only, без `status` — это медиа-артефакт), маршруты `POST/GET /api/agents/generate-image`. Раздача файлов на деве — `express.static` на `/media` (`backend/src/index.ts`), `frontend/vite.config.ts` проксирует `/media` тем же способом, что `/api` — в проде эту роль возьмёт nginx/Caddy (раздел 7). **UX доработан итеративно по правке пользователя после первого живого теста:** клиенту не нужно видеть ни английский промпт, ни модель/цену генерации — только готовую иллюстрацию, и не нужно два отдельных клика (сначала промпт, потом картинка). `ImageGenerationBlock.tsx` заменяет собой стандартный рендер документа для этапа `visual-generator` целиком (`StagePanel.tsx`) — одна кнопка молча пишет свежий промпт и сразу вызывает генерацию, показывает только картинку; плюс кнопка «Скачать картинку» (`<a download>`, человекочитаемое имя файла) — закрывает требование раздела 6 «кнопки скачивания медиафайлов» для картинок. Живой тест на тестовом клиенте (обе площадки): реальные вызовы Seedream, картинки сохранены и отданы по `/media/...`, содержательные и без стоковых клише, скачивание подтверждено (проверено визуально пользователем).
- **Гейт подтверждения перед генерацией видео для Reels реализован (решение сессии 2026-09-01) — только текстовый промпт, без личных референсов клиента.** См. раздел 8, пункт 2. Новый агент `reelsVideoGenerator.ts` (`deepseek/deepseek-v4-flash`) пишет промпт по новому `prompts/reels-video-generator.md` — визуализирует только хук сценария `reels-writer` (первые 3 секунды), не всю раскадровку: видео-модели делают один короткий непрерывный клип, не смонтированный ролик из нескольких сцен — та же логика «демонстрация качества», что и с одной иллюстрацией на пост. `backend/src/lib/videoGeneration.ts` — прямой вызов OpenRouter `/api/v1/videos`, повторяет `generate_video` из `smm-mcp/src/index.js` (модель `kwaivgi/kling-v3.0-pro`, поллинг `polling_url` каждые 5 сек до 40 попыток, каталог `workspace/07-reels/` — уже раздаётся через существующий `/media`-маунт). **Сознательно без `reference_image`/`first_frame_url`/`last_frame_url`** — только текст промпта, даже если `reels_scripts.usedReferences: true`; поддержка реальных фото клиента в видео отложена до публичного домена/туннеля, код для неё не написан. `duration` фиксирован (5 сек, под масштаб хука), `aspect_ratio: "9:16"` всегда. Новые таблицы `reels_video_prompts`/`generated_videos` (append-only, зеркало картиночных без `platform` — Reels один клип на клиента). Фронтенд: `VideoGenerationBlock.tsx` — зеркало `ImageGenerationBlock.tsx` (те же CSS-классы, не дублируем стили), но добавляется **под** видимым сценарием рилса, не заменяет его — сам сценарий (хук/раскадровка/озвучка) реальный контент для клиента, скрывать нечего, скрыт только новый видео-промпт. Живой тест на тестовом клиенте: реальный вызов Kling v3.0 Pro ($0.84 — заметно дороже картинки за $0.04), видео сохранено и отдано по `/media/...`, содержательный кадр по хуку, скачивание подтверждено (проверено визуально пользователем — «огонь»).
- **Референсы для рилса перенесены с онбординга на страницу «Рилсы» — после сценария, не до него (решение сессии 2026-09-02, по прямому запросу пользователя).** Раньше клиент грузил фото на онбординге вслепую, до того как видел сценарий, и по 5 категориям (`before_after`/`workspace`/`showcase`/`products`/`process`), общим входом для `visual-style-analyzer` (картинки И рилсы). Пользователь: изначально референсы задумывались только для рилсов, и добавлять их стоит уже глядя на готовый сценарий, а не заранее вслепую — так рационально, так и делаем. Для картинок-постов эта связка (`visual-style-analyzer` → `visual-generator`) больше не будет использоваться — решение по ней явно отложено пользователем на отдельный разговор, в этой сессии `visualGenerator.ts`/`visualStyleAnalyzer.ts` не тронуты. Реализовано: новая независимая таблица `reels_reference_files` (без категорий — одна простая зона загрузки) и роуты `POST/GET /api/reels-references`, полностью отдельные от старой `reference_files`/`visual-style-analyzer` — сознательно, чтобы новые референсы рилса не утекали в анализ стиля для картинок через общую выборку `visual-style-analyzer` по всем референсам клиента разом. Пока это только хранение — не вызывает `visual-style-analyzer`, не передаётся в `generate_video` как `reference_image` (тот же нерешённый вопрос про публичный URL с localhost, что и раньше, см. раздел 8); wiring — отдельная будущая задача, если вообще понадобится. Фронтенд: секция «Материалы для контента» и компонент `ReferenceDropzone` удалены с онбординга целиком (не осталось неиспользуемого кода); новый `ReelsReferenceUpload.tsx` встроен в `StagePanel.tsx` между сценарием рилса (`RunBlock`) и кнопкой генерации видео (`VideoGenerationBlock`) — показывается только после того, как сценарий уже сгенерирован. Живой тест backend-эндпоинтов подтвердил цикл загрузка → список; `tsc`/`vite build` обоих пакетов проходят чисто.
- **Превью и удаление референсов рилса добавлены сразу же (тот же день, по запросу пользователя — «добавила чтобы проверить, а удалить не могу»).** `DELETE /api/reels-references/:id` (сверяет `clientId`, чтобы нельзя было удалить чужой файл по id) удаляет и строку в `reels_reference_files`, и файл с диска. Новый статический маршрут `/uploads` (`express.static`, зеркало `/media`) — референсы и так задуманы публично раздаваемыми по разделу 7 спецификации, просто раньше это не было реализовано технически; `GET`/`POST /api/reels-references` теперь возвращают `publicUrl`. Фронтенд: `ReelsReferenceUpload.tsx` показывает реальные миниатюры (`<img>`, было — просто имя файла текстом) с кнопкой «✕» поверх каждой. Отдельной ручки «заменить» нет — замена = удалить и загрузить заново, тот же паттерн, что уже используется в остальном кабинете («Переделать» вместо редактирования на месте). Живой тест: загрузка → превью реально отдаётся по `/uploads/...` (200) → удаление (204) → файл и запись пропадают (404 на повторный запрос) → повторное удаление того же id корректно даёт 404, не 500.
- **Два живых бага сразу же после показа пользователю: миниатюра не грузилась, имя файла — кракозябры.** (1) `frontend/vite.config.ts` проксировал на бэкенд только `/api` и `/media`, про новый `/uploads` (см. пункт выше) забыли — браузер в деве стучался в сам Vite-сервер, `<img>` не получал картинку, вместо неё показывался `alt`-текст. Добавлен третий прокси-блок `/uploads` → `http://localhost:3000`, тем же способом, что и `/media`. (2) Кириллическое имя файла хранилось и отображалось кракозябрами — busboy (используется multer) по умолчанию декодирует заголовки multipart как `latin1`, а не `utf8`. Исправлено в `reelsReferences.ts`: `Buffer.from(req.file.originalname, "latin1").toString("utf8")` перед сохранением в БД и возвратом в ответе (тот же баг латентно жил и в старом онбординговом эндпоинте загрузки референсов, но там имя файла нигде не отображалось — не трогали, вне фокуса этой задачи). Живой тест через реальный HTTP-запрос с кириллическим именем файла (`Снимок экрана.png`) подтвердил оба фикса: имя сохранилось и вернулось корректно, `/uploads/...` отдаёт картинку и напрямую с бэкенда (3000), и через прокси Vite (5173).
- **Визуальный стиль для картинок-постов теперь синтезирует `account-packager` из текста, а не `visual-style-analyzer` из фото клиента (решение сессии 2026-09-02, отвечает на отложенный с прошлой сессии вопрос «что с картинками»).** Пользователь объяснила замысел: `account-packager` получает новый Шаг 4 «Визуальный стиль» — палитра, свет, композиция и чего избегать, выведенные из позиционирования, ЦА и метода (плюс, если есть, заземление по факту через `Аудит шапки профиля` — тем же принципом, что уже стоит в Шаге 5 про рекомендации по шапке), и эта рекомендация не просто отображается в документе «Упаковка профиля», а **передаётся дальше** в `visual-generator`, чтобы держать единый стиль иллюстраций на будущее (не только для 3 демо-позиций MVP). Это **полная замена** прежнего источника (`ensureVisualStyleProfile`/референсы клиента) для картинок-постов — решение пользователя, подтверждает и завершает раннюю пометку «для фото они [референсы] использоваться не будут» (см. предыдущую запись про референсы рилса). `visual-style-analyzer` как агент **не тронут и не удалён** — по решению пользователя оставлен как есть (используется только `reels-video-generator`, для картинок больше не вызывается вообще).

  Реализовано: `prompts/account-packager.md` — новый Шаг 4 (палитра/свет/композиция/чего избегать, с той же логикой заземления по `Аудиту шапки профиля`, что и у соседнего Шага 5, который сдвинут с 4 на 5), обновлены «Структура тела документа» и текст блока «Следующий шаг» (одна строка — «Визуальный стиль отсюда используется дальше при подготовке иллюстраций к постам»). `prompts/visual-generator.md` — вход теперь `Упаковка профиля` целиком (релевантен только раздел «Визуальный стиль», буквально), ветка «профиля нет» убрана вместе с полем `визуальный_профиль_использован` в frontmatter — упаковка профиля обязательный вход (жёсткая предпосылка, как и для `copywriter`), ветки «нет» не бывает. Код: `visualGenerator.ts` больше не зовёт `ensureVisualStyleProfile`, вместо этого сам получает *последнюю версию* `packaging_profiles` тем же паттерном, что и `copywriter.ts` (независимый повторный fetch, не доверие транзитивной гарантии через `content_plan`/`copywriter_post`), с собственным `PrerequisitesMissingError(["account-packager"])`. Таблица `visual_generator_prompts`: колонки `usedVisualProfile`/`visualStyleProfileVersion` удалены, добавлена обязательная `packagingProfileVersion` — миграция `0019_normal_captain_flint.sql` написана вручную (DROP + CREATE TABLE), потому что `drizzle-kit generate` для этой правки требует интерактивного TTY-подтверждения (не переименование ли столбца), недоступного в этой среде (см. «Известные грабли»); 8 старых тестовых строк в таблице (синтетические e-mail вроде `reels-live-test@example.com`, не данные реального клиента — проверено запросом к БД перед удалением) ушли вместе с пересозданием таблицы. Живой тест на существующем тестовом клиенте: `account-packager` v2 выдал содержательный раздел «Визуальный стиль» (тёплая приглушённая палитра, мягкий рассеянный свет, просторная минималистичная композиция — выведено из метода «без драматизации» и сегмента, которому важна безопасность) и честно отказался опираться на `Аудит шапки профиля`, когда в нём оказались чужие тестовые каналы (тот же паттерн защиты, что уже проверялся раньше на этой же паре агентов); `visual-generator` v1 буквально использовал эту палитру в готовом английском промпте для картинки (`dusty green`, `beige`, `soft diffused light`, `spacious minimal composition`) — цепочка передачи данных подтверждена не совпадением, а прямым текстовым соответствием.
- **Закрыта давняя orchestration-дыра: `visual-style-analyzer` никогда не запускался через реальный кабинет (решение сессии 2026-09-01).** Существовал только внутри `run-all` (недоступен из UI), поэтому реальный клиент никогда не получал анализ своих референсов. Решение — автозапуск изнутри потребителей результата (`visual-generator`, `reels-video-generator`), не отдельная кнопка и не пара с существующим этапом (в отличие от `profile-header-analyzer`, для него нашлась более простая пара с «Анализ аккаунта» — здесь нужны сами референсы, не own-ссылки). Новая `ensureVisualStyleProfile(clientId)` (`backend/src/agents/visualStyleAnalyzer.ts`): если профиль уже есть — переиспользует, если нет — запускает `runVisualStyleAnalyzer` сама (некритичные ошибки — `undefined`, тот же `.catch`-паттерн, что у `profile-header-analyzer`). Запускается **только один раз** — референсы грузятся лишь на онбординге, флоу добавления из кабинета нет, повторный вызов на каждый клик «Сгенерировать» не добавил бы данных, только тратил бы деньги. Живой тест: без референсов — прежнее поведение (нейтральный дефолт); с двумя тестовыми референсами — первый вызов `visual-generator` создал профиль автоматически, второй (другая площадка) переиспользовал ту же версию, не запустив анализ повторно (подтверждено запросом к БД).
- **Кнопка «Переделать» убрана полностью, реализовано сразу — не отложено до деплоя, как планировалось раньше (решение сессии 2026-09-02, по прямому запросу пользователя: «это демо, нечего играться за мой счёт»).** Расширяет прежнее более узкое решение (пять «честных» аналитических этапов) на все текстовые этапы без исключения, включая `content-planner`, который раньше был явно исключён («план — более гибкий документ»). `RunBlock` (`StagePanel.tsx`, общий компонент для всех текстовых этапов) теперь показывает кнопку запуска только пока результата ещё нет — как только он появился, кнопки нет вообще, повторный запуск с фронтенда невозможен ни для одного текстового этапа.

  Медиа-генерация решена отдельно от текстовых этапов, с разным исходом для картинок и видео. Пользователь в этот же день показала пример неудачной генерации картинки (реальный демо-JPG: фигура человека, обрубленная на торсе без плеч и шеи, вплотную к рулю без ощущения глубины между ними — не художественный кадр, а брак композитинга/анатомии) как аргумент — без возможности перегенерировать демо-клиент получил бы именно такой результат окончательным. Решение: **у картинок кнопка «Сгенерировать заново» остаётся** ($0.04/вызов, не критично для демо-бюджета); **у видео кнопки регенерации больше нет** ($0.84/вызов — слишком дорого), первая генерация становится окончательной. Правка `prompts/visual-generator.md` с явным запретом на «обрубленные» фигуры была сделана и в тот же день отменена: следующая живая генерация (тестовый клиент-йогастудия, отдельная запись ниже) дала кадр с человеком без верхней части лица — не тот же брак, но явное следствие новой формулировки «не обязательно с лицом». Пользователь отменила правку целиком («невозможно предусмотреть каждый косяк при генерации, чем больше ограничений — тем больше путаешь модель, смещая акценты»); промпт вернулся к состоянию до этой сессии.
- **Живой баг: двоеточие внутри значения `тема:` ломает весь YAML-frontmatter, не только это поле — найдено пользователем на скриншоте («Посты» выглядят «уныло»), не тестом.** `copywriter.md`/`reels-writer.md` копируют тему дословно из контент-плана, а темы там часто в формате «Формат: суть» («Личная история: момент, когда я сам заметил автопилот») — само значение содержит `:`, невалидный YAML (парсер читает как вложенный маппинг). `js-yaml.load()` падает на **всём** блоке, не только на одном поле — оба места, что полагаются на успешный `load()` для валидации найденного `---...---` блока (`backend/src/lib/frontmatter.ts`, `frontend/src/lib/markdown.ts`), решают «это не настоящий frontmatter» и пропускают его: бэкенд не может подставить корректный статус, фронтенд не срезает frontmatter вообще — весь сырой YAML утекает в интерфейс текстом. Исправлено в обоих файлах: если прямой `load()` падает, повторная попытка с автоматическим заключением в кавычки значений, которые сами содержат `:` — не переписывает сохранённый документ, только для валидации при парсинге. Плюс оба промпта теперь просят модель квотировать `тема` сама, если в ней есть двоеточие. Подтверждено живым тестом на реальном сломанном документе из БД (не синтетическом).
- **Карточка готового поста в кабинете дособрана визуально — по прямому запросу пользователя после скриншота, «бледный текст, ни грамма цвета, скучно».** Новая карточка `.stage-document-post` только для `copywriter` (`StagePanel.tsx`): мягкий сливовый фон, кружок-„ (тот же приём и та же вёрстка через `flex`, что уже в `InterviewCard.tsx` онбординга), текст в editorial-serif. Аналитические этапы (ЦА, экспертность и т.д.) не тронуты — там по брифу важна скорость и ясность, не убеждение.
- **`content-planner` и `copywriter` переведены с DeepSeek V4 Flash на DeepSeek V4 Pro (решение сессии 2026-09-02).** По прямому запросу пользователя после прочтения реального вывода — примеры названы «полным бредом»: несвязные образы, натянутые метафоры, внутренние противоречия в самом тексте («за час... за 5 минут»). Оба агента синтезируют поверх уже готовых документов (не мета-анализ с нуля, как у `audience-unpacker`/`expertise-unpacker`), поэтому Flash изначально казался достаточным — но связность самого генерируемого текста оказалась недостаточной на практике. Меняется только константа `MODEL` в обоих файлах, промпты не тронуты. Сравнительного теста Pro/Flash не проводилось — решение по одному явному примеру.

- **Results-страница реализована (решение и код сессии 2026-09-03) — read-only витрина готового демо, отдельная от кабинета.** По прямому запросу пользователя: раз ссылку можно переслать людям, которые продукт ещё не видели («потенциальные лиды»), страница сделана презентационной — ближе по тону к Брифу 1 (лендинг: тепло, editorial-асимметрия), не к Брифу 2 (кабинет: скорость и ясность). Показывает только готовый демо-контент — посты, картинки, сценарий рилса и видео; **аналитические документы (ЦА, экспертность, анализ аккаунта/конкурентов, упаковка) сознательно не включены** — это стратегические данные клиента о собственной аудитории и позиционировании, шэрить их случайным зрителям по пересланной ссылке не то же самое, что показать готовый пост (решение принято, не обсуждалось отдельно — при возражении легко расширить).

  Доставка автоматизирована и переиспользует email-инфраструктуру из раздела 2: новая `backend/src/agents/resultsDelivery.ts` (`ensureResultsLinkSent`) вызывается «на удачу» (fire-and-forget, ошибки не блокируют ответ) после каждого сохранения поста/сценария рилса (`routes/agents.ts`, POST `copywriter`/`reels-writer`) — идемпотентно проверяет, готов ли уже весь текстовый демо-контент под реальные площадки клиента (посты на все свои платформы + сценарий, если есть ВК; пустой список площадок — явно не «готово», та же защита от вакуумной истины, что уже стоит на фронтенде), и если да, а results-ссылка для клиента ещё не существует — создаёт её (`admin/resultsLink.ts`, `generateResultsLink`, `kind: "results"`, TTL 90 дней, не сгорает при использовании) и отправляет на email клиента; если SMTP не настроен — тем же фолбэком, что и у одобрения заявки, уведомление оператору для ручной пересылки. `GET /api/results/:token` (`routes/results.ts`) — без `requireSession` (токен в URL и есть авторизация), собирает *последние версии* постов/картинок/сценария/видео клиента напрямую по `clientId` из ссылки.

  **Живой баг сразу же при первой проверке: эндпоинт отвечал `401 no_session`, хотя не требует сессии вообще.** Причина — порядок монтирования роутеров в `index.ts`: `onboardingRouter`/`agentsRouter`/`reelsReferencesRouter` регистрируют `router.use(requireSession)` без пути, который матчит вообще любой путь, доходящий до этого роутера, и отвечает 401 сам, не вызывая `next()` — а Express передаёт запрос в каждый смонтированный через `app.use("/api", ...)` роутер по порядку регистрации независимо от того, есть ли в нём подходящий маршрут. `resultsRouter`, смонтированный последним, просто не успевал получить запрос — до него перехватывал один из трёх предыдущих. Исправлено — `resultsRouter` перемещён сразу после `accessRouter` (оба публичные, без сессии), до всех сессионных роутеров.

  **Второй, более серьёзный найденный баг — не в новом коде, а в старой ручной правке `_journal.json` из прошлой сессии (`0019_normal_captain_flint.sql`, обход TTY-ограничения `drizzle-kit generate`).** Две новые миграции этой сессии (`0020` — `access_requests.name`, `0021` — `clients.name`) сгенерировались и «применились» без единой ошибки (`npm run db:migrate` дважды подряд печатал «Migrations applied to ...»), но колонки физически не появились в БД — вскрыто прямой проверкой `PRAGMA table_info`, не тестом. Причина — мигратор drizzle-orm сравнивает не порядковый индекс миграций, а `when` из `_journal.json` (`folderMillis`) с `created_at` последней уже применённой строки в `__drizzle_migrations`, и применяет только миграции строго новее; у вручную вписанной `0019` `when` был выставлен на круглое время в будущем (`15:00:00.000Z` в день записи) — обе следующие миграции, сгенерированные часом раньше по факту, получили меньший `when` и были молча пропущены. Поднятие `when` у `0020`/`0021` выше значения `0019` и повторный `db:migrate` всё исправили. См. подробный урок в CLAUDE.md, «Известные грабли» — правило на будущее: при любой ручной правке журнала сверять `when` с `created_at` последней реально применённой миграции, не с текущим временем на глаз.

  Живой тест на существующем тестовом клиенте (оба поста, картинки на обе площадки, сценарий рилса и видео) подтвердил: `GET /api/results/:token` отдаёт полный бандл (`platforms`, `posts[]` с темой/текстом/картинкой, `reels` с текстом/видео), несуществующий токен — честный `404 link_not_found`, страница `/results/:token` рендерится через dev-прокси фронтенда.
- **Панель экспорта сужена и реализована в тот же день (решение сессии 2026-09-03).** Пользователь решила: редактируемое текстовое поле из раздела 6 не нужно вообще — клиент копирует текст и правит его сам, вне инструмента (не в кабинете). Из панели остались «Копировать текст» и скачивание — скачивание картинок/видео/контент-плана уже было, не хватало только текста постов. Реализовано: `PostCard` в `frontend/src/components/StagePanel.tsx` (заменил инлайновый рендер копирайтер-карточки) — под карточкой поста две `quiet`-кнопки, «Копировать текст» (`navigator.clipboard.writeText`, временная подпись «Скопировано») и «Скачать текст (.txt)» (тот же `Blob`+`<a download>`-паттерн, что уже у `.xlsx`-экспорта контент-плана, `lib/contentPlanExport.ts`). Обе читают текст через `ref.innerText` с уже отрендеренного `ReactMarkdown`, не из сырого `documentMarkdown` — в буфер/файл уходит то, что клиент видит на экране, без `**` и прочего markdown-синтаксиса. Только для постов в кабинете — сценарий рилса и results-страница не тронуты (пользователь просила конкретно посты, по аналогии с уже существующими кнопками скачивания).

### Следующие шаги (в порядке приоритета)

1. **Остальной фронтенд** — лендинг, контент-хаб (раздел 6). Онбординг, кабинет эксперта, results-страница и панель экспорта (в сужённом виде) — готовы.
2. **Гейт подтверждения перед медиа-генерацией** (раздел 3, Шаг 4) — **картинки и видео реализованы (решения сессий 2026-09-01), реальные личные референсы в видео — ещё нет.** `POST/GET /api/agents/generate-image`/`generate-video` вызывают OpenRouter `/api/v1/images`/`/api/v1/videos` напрямую из бэкенда (`backend/src/lib/imageGeneration.ts`/`videoGeneration.ts`), не через `smm-mcp` — тот остаётся MCP stdio-сервером для Claude Code, не HTTP-сервисом для продакшн Express (архитектурный вопрос решён именно так, применён одинаково для картинок и видео). Видео решает первую из двух проблем, названных изначально (`reels-writer` не писал английский промпт для `generate_video`) — новый агент `reels-video-generator` пишет промпт, визуализирующий только хук сценария (не всю раскадровку — видео-модели генерируют один короткий непрерывный клип, не смонтированный ролик). Вторая проблема (публичный URL для референсных кадров, недоступный на `localhost` в деве) **сознательно обойдена, не решена**: видео генерируется только по текстовому промпту, без единого `reference_image`/`first_frame_url`/`last_frame_url`, даже если сценарий концептуально опирается на личные материалы клиента — поддержка реальных референсов в видео отложена до публичного домена/туннеля. Подробности реализации обоих гейтов — раздел 9.
3. ~~`run-all` как фоновая задача/SSE~~ — закрыто (см. раздел 8): кабинет работает кнопками на каждом этапе, продуктового пути к `run-all` нет, переделывать незачем.

### Архитектурное решение этой сессии (не пересматривать без повода)

Код агентов и промпты **не колоцированы** в одну папку на агента: `prompts/*.md` — самостоятельные документы (промпт задуман так, что эксперт может подгрузить его и в любую другую нейросеть напрямую, не только использовать через этот продукт), `backend/src/agents/*.ts` — код-оркестрация поверх них. Разделение существовало в структуре репозитория до начала бэкенд-разработки, сознательно сохранено при подключении первого агента.

### Агенты, добавленные сверх исходной таблицы раздела 4

`visual-style-analyzer` не входил в исходную спецификацию — введён в этой сессии по итогам обсуждения `visual-generator`: без разового анализа визуальной айдентики клиента генерации картинок от раза к разу рисковали расходиться по стилю (нейрослоп/сток вперемешку со своим). Раздел 3, Шаг 1 упоминал «визуальную айдентику» как часть авто-анализа стиля, но она не была реализована — `account-analyzer` разбирает только текст. `visual-style-analyzer` закрывает именно эту часть, отдельным агентом (не расширением `account-analyzer`), так как вход и модель принципиально другие (изображения, vision-модель, а не текст постов).

`profile-header-analyzer` тоже не входил в исходную спецификацию — введён по итогам того же обсуждения (раз vision-анализ уже есть в проекте, логично применить его и к реальной шапке профиля клиента, не только к загруженным референсам). Отдельный агент, а не расширение `visual-style-analyzer` или `account-packager`: вход и модель здесь снова принципиально другие (не референсы клиента, а реальные аватар/обложка/описание с самих площадок, требующие нового парсера — `groups.getById` для ВК, HTML `t.me/s/` для Telegram), и это фактологический анализ, отдельный от синтеза рекомендаций в `account-packager`, — тот же принцип разделения «анализатор описывает факт → синтезатор превращает факт в рекомендацию», что уже использован во всём пайплайне.
